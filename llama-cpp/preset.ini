version = 12.0.1

[Qwen3.8-27B]
alias = Qwen3.8 27B
model = /home/llama-cpp/.cache/huggingface/hub/unsloth/Qwen3.8-27B-GGUF/Qwen3.8-27B-UD-Q6_K_XL.gguf
mmproj = /home/llama-cpp/.cache/huggingface/hub/unsloth/Qwen3.8-27B-GGUF/mmproj-F16.gguf
cache-type-k = q8_0
cache-type-v = q8_0
ctx-size = 262144
flash-attn = on
min-p = 0.0
n-gpu-layers = all
presence-penalty = 0.0
reasoning = auto
reasoning-effort = medium
reasoning-preserve = off
repeat-penalty = 1.0
spec-draft-n-max = 2
spec-draft-ngl = all
spec-type = draft-mtp
split-mode = tensor
temp = 1.0
top-k = 20
top-p = 0.95

[Qwen3.6-35B-A3B]
alias = Qwen3.6 35B A3B
model = /home/llama-cpp/.cache/huggingface/hub/unsloth/Qwen3.6-35B-A3B-MTP-GGUF/Qwen3.6-35B-A3B-UD-Q8_K_XL.gguf
mmproj = /home/llama-cpp/.cache/huggingface/hub/unsloth/Qwen3.6-35B-A3B-MTP-GGUF/mmproj-F16.gguf
cache-type-k = q8_0
cache-type-v = q8_0
ctx-size = 262144
flash-attn = on
min-p = 0.0
n-gpu-layers = auto
presence-penalty = 0.0
reasoning = auto
reasoning-preserve = off
repeat-penalty = 1.0
spec-draft-n-max = 2
spec-type = draft-mtp
split-mode = layer
temp = 0.6
top-k = 20
top-p = 0.95

[Qwen3.5-2B]
alias = Qwen3.5 2B
model = /home/llama-cpp/.cache/huggingface/hub/unsloth/Qwen3.5-2B-MTP-GGUF/Qwen3.5-2B-Q8_0.gguf
mmproj = /home/llama-cpp/.cache/huggingface/hub/unsloth/Qwen3.5-2B-MTP-GGUF/mmproj-F16.gguf
cache-type-k = q8_0
cache-type-v = q8_0
ctx-size = 32768
flash-attn = on
main-gpu = 1
min-p = 0.00
presence-penalty = 1.5
reasoning = off
repeat-penalty = 1.0
spec-draft-n-max = 2
spec-type = draft-mtp
split-mode = none
temp = 1.0
top-k = 20
top-p = 0.95

[Qwen3-Embedding-0.6B]
alias = Qwen3 Embedding 0.6B
model = /home/llama-cpp/.cache/huggingface/hub/Qwen/Qwen3-Embedding-0.6B-GGUF/Qwen3-Embedding-0.6B-f16.gguf
cache-type-k = q8_0
cache-type-v = q8_0
ctx-size = 16384
embedding = on
flash-attn = on
main-gpu = 1
spec-draft-n-max = 0
spec-type = none
split-mode = none

[Qwen3.8-Flash-Next]
alias = Qwen3.8 Flash Next
model = /home/llama-cpp/.cache/huggingface/hub/unsloth/Qwen3.8-Flash-Next-GGUF/UD-Q4_K_XL/Qwen3.8-Flash-Next-UD-Q4_K_XL-00001-of-00004.gguf
mmproj = /home/llama-cpp/.cache/huggingface/hub/unsloth/Qwen3.8-Flash-Next-GGUF/mmproj-F16.gguf
cache-type-k = q8_0
cache-type-v = q8_0
ctx-size = 262144
flash-attn = on
# The 26.82 GiB n-gram table is a single `per_layer_token_embd` tensor read host-side through
# `ggml_get_rows`, so it never enters VRAM. That plus the CPU-side experts is ~60 GiB of host memory.
# NOTE: `none` means "no special loading mode", NOT a read-in - mmap stays on either way (46 GiB of
# the process is file-backed, measured). It is still the right value: `mlock` measured 45-47 t/s
# against 68.6 for `none`.
load-mode = none
min-p = 0.0
# Blocks 0-27 keep their routed experts in RAM. Four blocks more than the 24 this ran at before MTP:
# the draft head has to fit on GPU 1, and 4 x 1.465 GiB of expert weights is what buys the room.
# Measured downward: 26 is no faster and eats GPU 0's spare (0.4 GiB left, and GPU 0 is the image-gen
# card), 24 fails to load. Decode is host-CPU-bound here, not PCIe-bound - the GPUs idle at 14-36%
# busy while 11.8 of 12 cores are pegged - so moving experts back to VRAM buys nothing.
n-cpu-moe = 28
n-gpu-layers = 99
presence-penalty = 0.0
reasoning = auto
reasoning-effort = medium
reasoning-preserve = off
repeat-penalty = 1.0
# 2.58 GiB draft head, loaded as a separate model. `shared-` means it carries no `token_embd` or
# `output.weight` and borrows the target's, saving 1.27 GiB; the loader takes a raw pointer into the
# target's tensor map, so the head must land on whichever card holds `output.weight` - GPU 1, the
# last slot of the split. Q8_0 over Q4_K_M: 2 points more acceptance, and a draft step is dominated
# by the output projection, which is cheaper at 8 bits than at 4. Requires the pinned fork build
# (see .env.example) - mainline silently ignores this key.
spec-draft-model = /home/llama-cpp/.cache/huggingface/hub/unsloth/Qwen3.8-Flash-Next-GGUF/MTP/mtp-Qwen3.8-Flash-Next-shared-Q8_0.gguf
# Depth 3, measured over 10 fresh loads per depth (`bench --loads 10`). Depth 2 medians 46.1 t/s and
# scatters 38.8-64.3 (55% spread); depth 3 medians 57.4 and 12 of 13 loads land in 56.7-58.2, one
# outlier at 49.6, so it also largely suppresses the per-process throughput lottery; depth 4 matches
# the median but 3 of 10 loads collapse to 31-43. The old "3 and 4 are slower than no speculation"
# note was measured with a single warm-up on one load and was wrong.
spec-draft-n-max = 3
spec-draft-ngl = all
spec-type = draft-mtp
split-mode = layer
# `tensor-split` divides by LAYER COUNT, and `n-cpu-moe` leaves the first 28 layers nearly
# weightless, so an even-looking split lands almost every heavy block on one card - 52,48 put 22
# heavy blocks plus the output head on GPU 1 and OOMed. 78,22 gives GPU 0 layers 0-38 (28 light +
# 11 heavy) and GPU 1 layers 39-47, the output head and the draft head. GPU 1 stays the lighter
# side because it also hosts the embedding model and Qwen3.5-2B, which pin `main-gpu = 1` and cost
# ~6 GiB whenever they are resident. Leaves ~2.4 GiB spare on GPU 0 and ~1.5 GiB on GPU 1.
tensor-split = 78,22
temp = 1.0
top-k = 20
top-p = 0.95
