{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Configuration schema for models",
  "properties": {
    "models": {
      "description": "List of models",
      "items": {
        "additionalProperties": false,
        "properties": {
          "name": {
            "description": "Name of the model",
            "type": "string"
          },
          "file": {
            "description": "Model file name",
            "type": "string"
          },
          "mmproj": {
            "description": "Model multimodal project file name",
            "type": "string"
          },
          "repository": {
            "description": "Repository containing the model",
            "type": "string"
          },
          "thinking": {
            "description": "Whether the model has thinking capabilities",
            "type": "boolean"
          },
          "vision": {
            "description": "Whether the model has vision capabilities",
            "type": "boolean"
          }
        },
        "required": ["name", "repository", "file", "thinking", "vision"],
        "type": "object"
      },
      "type": "array"
    },
    "version": {
      "description": "Configuration version",
      "type": "string"
    }
  },
  "required": ["version", "models"],
  "title": "Models Configuration",
  "type": "object"
}
