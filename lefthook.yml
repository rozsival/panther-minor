commit-msg:
  jobs:
    - name: commitlint
      run: pnpm commitlint --edit {1}

pre-commit:
  parallel: true
  jobs:
    - run: pnpm run build:cli
      glob:
        - "bin/src/bashly.yml"
        - "bin/src/**/*.sh"
        - "!bin/src/lib/send_completions.sh"
      stage_fixed: true

    - run: pnpm ultracite fix
      glob:
        - "**/*.js"
        - "**/*.md"
        - "**/*.json"
        - "**/*.jsonc"
      stage_fixed: true
